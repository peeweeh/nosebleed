# AWS Bedrock credentials — copy this to .env.local (gitignored)
# Uses the named AWS profile from ~/.aws/credentials
AWS_PROFILE=your-aws-profile
AWS_REGION=us-east-1
AWS_MODEL_ID=your-bedrock-model-id
